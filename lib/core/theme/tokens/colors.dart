import 'package:flutter/material.dart';

class TibebColors {
  // Core identity
  static const ink = Color(0xFF0B0F14);
  static const surface = Color(0xFF111826);
  static const paper = Color(0xFFE8E6E3);

  // Wisdom (primary identity)
  static const wisdom = Color(0xFFC8A24A);

  // Interaction
  static const blue = Color(0xFF4C7DFF);

  // Status
  static const success = Color(0xFF3CCB7F);
  static const warning = Color(0xFFF4B740);
  static const error = Color(0xFFFF5A5F);
}